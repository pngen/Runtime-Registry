// Index maintenance and secondary-index verification.
// The registry keeps canonical records as the source of truth and rebuilds
// derived indexes deterministically; see registry.cpp for the implementation.
#include <runtimeregistry/registry.hpp>
