function(rr_add_test name)
  add_executable(${name} ${name}.cpp)
  target_link_libraries(${name} PRIVATE RuntimeRegistry::runtimeregistry)
  target_include_directories(${name} PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
  add_test(NAME ${name} COMMAND ${name})
endfunction()

rr_add_test(test_identity)
rr_add_test(test_registry)
rr_add_test(test_protocol)
rr_add_test(test_leases)
rr_add_test(test_query)
rr_add_test(test_adversarial)
rr_add_test(test_concurrency)
add_executable(test_multiprocess test_multiprocess.cpp)
target_link_libraries(test_multiprocess PRIVATE RuntimeRegistry::runtimeregistry)
target_include_directories(test_multiprocess PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
add_test(NAME test_multiprocess COMMAND test_multiprocess $<TARGET_FILE:runtime-registry-coordinator> $<TARGET_FILE:runtime-registry-worker>)
